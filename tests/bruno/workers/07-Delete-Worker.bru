meta {
  name: Delete Worker
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/workers/{{createdWorkerId}}
  body: none
  auth: none
}

headers {
  client_id: {{clientId}}
  client_api_key: {{clientApiKey}}
}

tests {
  test("Status code is 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
  
  test("Worker is deleted", function() {
    // Try to get the worker - should return 404
    // Note: This is a separate request, so we just verify the delete succeeded
    expect(res.getStatus()).to.equal(204);
  });
}


