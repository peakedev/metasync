meta {
  name: 04-Pause-Run
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/runs/{{runId}}/pause
  body: none
  auth: none
}

headers {
  client_id: {{clientId}}
  client_api_key: {{clientApiKey}}
}

assert {
  res.status: eq 200
  res.body.status: eq PAUSED
}

