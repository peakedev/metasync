meta {
  name: Update Job Status - Error Consuming to Processed
  type: http
  seq: 12
}

patch {
  url: {{baseUrl}}/jobs/{{createdJobId}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  client_id: {{clientId}}
  client_api_key: {{clientApiKey}}
}

body:json {
  {
    "status": "PROCESSED"
  }
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Job status is updated to PROCESSED", function() {
    const job = res.getBody();
    expect(job.status).to.equal('PROCESSED');
  });
}
