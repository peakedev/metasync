meta {
  name: Error - Unauthorized (List Prompts)
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/prompts
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("Status code is 401", function() {
    expect(res.getStatus()).to.equal(401);
  });
  
  test("Response contains error message", function() {
    expect(res.getBody()).to.have.property('detail');
  });
}
