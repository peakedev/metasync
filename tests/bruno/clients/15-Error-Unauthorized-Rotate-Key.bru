meta {
  name: Error - Unauthorized (POST /clients/{id}/rotate-key)
  type: http
  seq: 15
}

post {
  url: {{baseUrl}}/clients/test-client-id/rotate-key
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("Status code is 401", function() {
    expect(res.getStatus()).to.equal(401);
  });
  
  test("Response contains error message", function() {
    expect(res.getBody()).to.have.property('detail');
  });
}

